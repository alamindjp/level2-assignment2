export type TTags = string;

export type TVariants = {
  type: string;
  value: string;
};
export type TInventory = {
  quantity: number;
  inStock: boolean;
};

export type TProduct = {
  email: string;
  productId: string;
  price: number;
  quantity: number;
};
